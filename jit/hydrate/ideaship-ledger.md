# KitJS V2 — Sổ theo dõi thi hành spec (LEDGER)

> `ideaship-final.md` là cửa chính: nó nói **cái gì** đã chốt. Sổ này nói **đã làm tới đâu** — cho
> từng mục đã chốt, trạng thái thật trong code ở ba nơi phải đồng ý với nhau: bộ kiểm Go
> (`jit/javascript/scan.go`, `jit/hydrate/compile.go`), hydrate kernel (`jit/hydrate/kernel.js`,
> `/kit.js`) và runtime component KitJS (`jit/javascript/src/*`). Cập nhật sổ trong cùng commit với
> code; một dòng ghi "xong" mà không có test đi kèm là dòng sai.
>
> Bắt đầu 21/09/2026, sau khi phát hiện runtime component được xây lệch spec (30+ component của
> catalogue kitwork.io/components làm theo runtime, không theo spec).

## Trạng thái từng mục đã chốt (§8 của FINAL)

| # | Mục đã chốt | Spec | Kernel `/kit.js` hôm nay | Runtime component hôm nay | Trạng thái |
| :-: | :--- | :--- | :--- | :--- | :--- |
| 1 | Binding 3 nhóm | `data-kit-bind:<name>` — **một dạng**, tên quyết định nhóm (Quốc chốt 21/09: bỏ `data-kit-attr`, vì hai tên trùng việc). Reflected boolean → property + attribute · live property → property · còn lại (aria-*, data-*, tên có gạch nối) → attribute | ✅ `writeBinding` trong kernel; SSR `PreRenderBind` bake theo cùng luật | ✅ `dom.js` `writeBinding`; scanner từ chối dạng list và đích không an toàn | ✅ 21/09 — test: `binding_groups_browser_test.go`, `bind_dom_test.go`, `scan_test.go`; 405 thuộc tính trên 12 site + fixture đã chuyển |
| 2 | Scope literal | `{ qty: 1, price: 250 }` hoặc `qty: 1, price: 250` — dấu `,`, **không** `;` | ✅ `boundaryScope` nhận dạng không ngoặc (`^tên:`) và bọc `{…}` rồi đưa vào CÙNG parser biểu thức — không parser thứ hai; dạng tên và dạng init `a = 1; b = 2` giữ nguyên | ✅ shorthand tách bằng `,`; gặp `;` báo `scope fields are separated by "," not ";"` (JS + `scope_seed.go`). Parser dữ liệu thuần vẫn giữ: giá trị phải là data, không biểu thức — hẹp hơn kernel, cùng ngữ pháp | ✅ 21/09 — test: `scope_literal_dom_test.go` (kernel), `scope_seed_test.go`, `scope_browser_test.go`; 89 thuộc tính trên 49 file (site, fixture, docs, cmd/gallery, cmd/kitui) đã chuyển |
| 3 | Alias = instance | `data-kit-alias="$modal"` | ✅ một cách viết: `data-kit-alias`; đuôi `name=$alias`, `data-kitwork-alias`, `data-alias` gỡ hẳn (`parseComponentTag` chỉ còn `name@version`; `ComponentName` Go chỉ cắt `@`; regex `jit/js/runtime.go` không nhận đuôi) | ✅ `data-kit-as` → `data-kit-alias` (src/component.js, drive.js, morph.js, directives.js); scanner từ chối `data-kit-as` là tên lạ | ✅ 21/09 — test: `app_surface_test.go` (kernel, kể cả 3 cách viết bị bác), `scan_test.go`, `prerender_bind_test.go`, `js/sidebar_test.go`; 247 chỗ trên 86 file + examples + huynhnhanquoc.com dashboard đã chuyển |
| 4 | Element có tên (trước: Ref) | `data-kit-element="search"` → `$element.search` (✎21/09 đổi từ `ref`/`$refs`: `ref` là viết tắt; số ít như 6 biến còn lại; gộp luôn tên bộ phận cho code component) | ✅ `$element` trong `elementScope`: registry của boundary sở hữu — `closest(SCOPE)` của phần tử đang hành động; tra cứu lúc đọc; tên thiếu → nullish | ✅ `$element` là local của MỌI action; `core.namedElements` = `ownedElements("[data-kit-element]")` của host/scope gần nhất (không có → trang); `context.element(name)` (cái đầu) + `context.elements(name)` (danh sách, thứ tự tài liệu) cùng bộ lọc owned(); element đi qua bảng đọc/động từ đóng của evaluator | ✅ 21/09 (đổi tên xong cùng ngày; phạm vi theo đề xuất) — test: `element_browser_test.go`, `context_element_browser_test.go`, `element_dom_test.go`, `scan_test.go` |
| 5 | 7 biến hệ thống | `$this`(~`$el`) `$host`(~`$root`) `$event` native `$error` `$element` (trước `$refs`) `$app` `$` | ✅ `elementScope(el, event)`: `$this`/`$el` = thẻ sở hữu directive, `$host`/`$root` = boundary (`closest(SCOPE)`, không có → `<html>`), `$event` = event native của handler, `$element`, `$app`, `$`; 9 tên dành riêng không được làm alias | ✅ local của mọi action: `$this`/`$el`, `$host` (host/scope gần nhất, không có → `<html>`), `$element`; `$event` = ảnh chụp đông cứng + `target`/`submitter`/`relatedTarget` là element thật (đi qua bảng element đóng) — KHÔNG phải object native; `$this`/`$el` thêm vào tên dành riêng (JS + `reservedAlias` Go) | ✅ 21/09 trừ `$error` (mục 8) — test: `system_variables_dom_test.go`, `system_variables_browser_test.go`, `scan_test.go`. `$this` chính / `$el` alias theo đề xuất; runtime component: `$event` không native được vì ngữ pháp đóng (xem nhật ký) |
| 6 | Modifier pipeline | `:window :document` → `:outside :escape :enter` → `:prevent` → `:stop` → `:debounce(n) :throttle(n)` → `:once` → chạy | ✅ họ sự kiện `data-kit-<event>[:mod…]` cho 11 event (trước chỉ `click`); `pipeline()` chạy đúng thứ tự cố định, filter hỏng KHÔNG nuốt event; `data-kit-away`/`data-kit-escape`/`data-kit-guard` GỠ (→ `click:outside`, `keydown:escape:window`, `:prevent`); `data-kit-debounce` chỉ còn cho `data-kit-model`; server `render.go` verify biểu thức + `checkEventModifiers` báo lỗi tại render | ✅ thêm `:window :document :throttle(n)`; `elsewhere()` gộp outside/window/document; `execute()` = prevent → stop → timing → chạy → once, `matches()` = filter trước; scanner Go cùng luật (đích một, self/outside không đi với window/document, debounce/throttle không đi cùng) | ✅ 21/09 — test: `events_test.go` + `modal_open_test.go` (kernel, có disable-check thứ tự filter/prevent và `:stop`), `render_test.go`, `event_conformance_browser_test.go`, `scan_test.go`. Runtime component giữ thêm `:self` (ngoài spec, nhóm filter) |
| 7 | `for` trên thẻ thường | `<li data-kit-for="item, i of items" data-kit-key="item.id">`; SSR ra `<!--kit-for:start id=x-->` + `data-kit-item="x"`; overlay `item index count first last even odd`; blueprint từ IR server; chung key resolver với morph | ✅ clone chính thẻ (đã có); overlay đủ 7 tên; danh sách nằm giữa `<!--kit-for:start id=fN-->` … `<!--kit-for:end-->`, mỗi hàng `data-kit-item="fN"` — đúng hình dây spec §7 | ✅ `prepareDirectFor`: thẻ thường mang `for` thành blueprint, thay bằng `<template>` cùng for/key rồi đi máy cũ (một bộ máy list); overlay đủ 7 tên; 5 từ overlay LEXICAL theo hàng — không lọt vào host component lồng (host giữ `count` riêng), tên item/index tác giả vẫn lọt; scanner nhận for/key trên thẻ thường | ✅ 21/09 phần CLIENT — test: `for_test.go` (kernel: overlay + marker), `direct_for_browser_test.go`, `structural_browser_test.go`, `scan_test.go`. CÒN MỞ (B): server materialize list (cần nguồn dữ liệu — PreRender chỉ có scope từ `data-kit-model`; danh sách SSR hôm nay là `{{ for }}` của template engine) + blueprint từ IR server + kernel nhận hàng SSR |
| 8 | `data-kit-error` + `$error` | boundary bắt lỗi component (lan truyền: CÒN TREO) | ✅ `guarded()` bọc mọi chỗ chạy biểu thức (text/show/bind/class/validate + pipeline sự kiện) → `reportError` tìm `closest("[data-kit-error]")`, chạy biểu thức boundary với `$error` {cause, message, directive, element}; một phần tử hỏng không làm gãy cả lượt render nữa; walker kernel vốn khoan dung (tên thiếu = 0, gọi non-function = undefined) nên boundary bắt lỗi do code component thật ném | ✅ `core.report(error, element, directive)` → `core.handleError` chạy action `data-kit-error` của boundary gần nhất với `$error`; các điểm report có element (action, binding text/show/bind/class/style, structure) đã truyền element; `readBinding` gắn tên directive; boundary ghi state trong lượt boot vẫn được vẽ lại (`core.booting`/`boundaryWrote`); scanner + validator nhận `data-kit-error` (mode action) | ✅ 21/09 THEO ĐỀ XUẤT: không lan quá boundary đầu; không boundary → console; handler tự ném → console + không tái nhập. `$error.recover()` của master KHÔNG làm (nghĩa chưa định) — test: `error_dom_test.go`, `error_boundary_browser_test.go`, `render_test.go`, `scan_test.go` |
| 9 | `show` → `hidden` | | ✅ | ✅ | ✅ |
| 10 | dirty-check, không đồ thị phụ thuộc | | ✅ | ✅ (`core.invalidate` theo record) | ✅ |
| 11 | conformance `walk ≡ eval` | corpus dùng chung, CI | ✅ 14 ca (`conformance_corpus.json`) | — | ✅ giữ, thêm ca theo từng mục |
| 12 | Core ≤ 12KB gzip, kiểm bằng CI | | ✅ `size_budget_test.go`: kernel.js qua ĐÚNG minifier production (`utilities/minifier.JS`, cái `work/jithydrate.go` dùng khi `!AllowLocal`) rồi gzip ≤ 12 KiB — hôm nay **9 365 / 12 288 byte** (sau C1). Bản `-tags stdminify` không gate (minifier passthrough). Ghi chú cũ "pipeline không có minifier" là SAI — production đã minify từ 2026-07; dev (ALLOW_LOCAL) phục vụ bản đọc được | — | ✅ 21/09 |

| 13 ✚ | `data-kit-seed` — DOM → state một lần (chốt với Quốc 21/09, gương của bind) | `seed="key"` text/JSON island · `seed:<name>="key"` theo 3 nhóm ngược · đích key / `a.b.c` / `list[]` · DOM thắng scope literal · không đoán kiểu | ✅ `seedElements()` trước mọi pass render; ghi qua `scopeFor(el)` (đúng chủ sở hữu trong chuỗi); `list[]` dựng lại khi có thành viên mới; lỗi → `reportError` | ✅ `seedBoundary()` trong dom.js trước bindings của record; host component: field phải khai báo (lỗi như `data-kit-scope`); once qua `record.seeded`; scanner + directives nhận `seed`/`seed:<name>`, đích được kiểm | ✅ 21/09 — server: `prerender_seed.go` đọc cùng seed vào page scope (ngoài boundary; JSON island; leaf text), `render.go` báo đích sai — test: `seed_dom_test.go`, `seed_browser_test.go`, `prerender_seed_test.go`, `render_test.go`, `scan_test.go` (đều có disable-check) |

## Còn treo — ĐÃ CHỐT 22/09 (Quốc: "vậy làm đi" = theo đề xuất; bảng 10 câu B ở `ideaship-final.md` §8)

B1 bỏ `$el`/`$root` ✅ · B2 `$event` native-shaped ở runtime component ✅ · B3 phạm vi `$element` = vùng gần nhất ✅ · B4 không `$error.recover()` ✅ · B5 `:self` vào Filter (kernel thêm) ✅ · B6 server materialize `for` hoãn · B7 `data-kit-style` kernel thêm khi có ca · B8 cắt dạng tên/init của `data-kit-scope` ✅ · B9 chính sách một kernel (ngữ pháp mới chỉ vào kernel) · B10 async nhánh B ✅ 22/09 chiều: **không có `effect`** — async sống trong component (method JS thật) hoặc Drive; kernel xếp một lượt vẽ khi scope bị ghi NGOÀI một lượt (timer/`.then`/callback), không xếp thêm khi ghi TRONG lượt (handler/model/render).

## Ngoài spec — phát sinh từ catalogue, chờ bàn sau khi A xong

- ~~Đặt tên phần tử của component~~ — GỘP vào `data-kit-element` 21/09: một attribute cho cả biểu thức (`$element.x`) và code component (`context.element(s)`); catalogue 20 marker tự chế (`data-carousel-track`…) chuyển dần sang `data-kit-element`, từng component khi có dịp.
- `data-kit-key` nguyên văn trên phần tử thường; `context.element()`; hợp đồng `contract` trong
  `kit.component`; scope lồng không leo lên host ngoài (`$alias` chỉ trong action).
- Hai runtime (`/kit.js` và runtime component) cùng đọc một ngữ pháp — spec §9 "chuỗi phép trừ"
  chưa làm; sổ này chỉ giữ hai bên **đồng ý về ngữ pháp**, chưa gộp.

## Nhật ký

- 22/09/2026 (chiều) — TRỪ HỆ JITJS (spec §9, không cần `effect` — Quốc: "tại sao phải cần nó khi chúng ta đã có morph… đi theo lối component first"): gỡ `data-kit(work)-action`/`-target`/`-trigger`, registry `behaviors`, `kit.fire/behavior/action/target/actions`, `bindVisible`, `compat.js`, `jit/js/lib/*` (10 verb); `jit/js/runtime.go` chỉ còn component (`jit/js/components`) + capability; Drive bỏ nhánh verb. 23 file site chuyển: toggle → `data-kit-scope="navOpen: false"` + `navOpen = !navOpen` + `show`/`bind:aria-expanded`; tab → `tab = 'x'` + `bind:aria-selected`/`bind:data-state` + `show`; dialog → `data-kit-element="search"` + `$element.search.showModal()/.close()`; copy → `data-kit-component="clipboard"` + `copy('…')`/`copy($element.x.textContent)` + `data-kit-class="copied ? 'is-copied' : ''"`; theme → `kit.theme.toggle()` (KHÔNG `$app.theme` — `$app` không do kernel cài, chỉ là alias component); form → `data-kit-drive="false"`. `more` 0 chỗ dùng. PHÁT HIỆN nhờ browser proof: method component ghi state ASYNC (`copied` trong `.then`/timer) không vẽ lại ở kernel (chỉ Promise trả về mới được `observeEffect` theo dõi) → thêm `wrote()`/`pass()`: ghi qua scope proxy ngoài lượt → `scheduleRender()` gộp; trong lượt không xếp thêm (B10 chốt). 98 biểu thức site compile qua twin Go. Kernel minified gzip **9 907** / 12 288. Test: `jit/js/migration_browser_test.go` (Chrome thật, 5 mẫu + clipboard stub), `write_repaint_test.go` (2 ca, cả hai có disable-check), `runtime_test.go`, `render_test.go`, `bridge_test.go`, `work/jithydrate_test.go`, `work/router_kitjs_test.go`.
- 22/09/2026 — B1–B10 chốt theo đề xuất; code: kernel bỏ `$el`/`$root` (runtime component bỏ `$el`), cắt dạng tên/init của `data-kit-scope` (báo lỗi "must be an object literal"), thêm `:self` (+ render.go ràng buộc); C2: kernel + `compile.go`/`eval.go` đọc `a[b]` (`"idx"` op; số → list/chuỗi, còn lại → tên member, tên chặn → nil) — corpus +6 ca, cả hai runner xanh, tắt parse `[` → client đỏ. Spec §2–§10 viết lại theo bộ đang chạy (3e99749f).
- 21/09/2026 — ĐỔI TÊN `ref` → `element` (mục 4): `data-kit-element`, `$element` (số ít), `context.element()`/`context.elements()`; `$refs` không còn là tên dành riêng; spec §2/§3/§6 sửa theo (đánh dấu ✎). Lý do: `ref` là viết tắt; số ít như 6 biến còn lại; một cơ chế đặt tên cho cả biểu thức lẫn code component.
- 21/09/2026 — SEED (mục 13, ngoài spec gốc, chốt với Quốc): cả hai runtime + scanner + server. Quyết định kèm: không `state`/`props` (alias + scope lexical đủ; liên kết cha–con thuộc câu một-kernel); `data-kit-ref` → `data-kit-element`/`$element` + `context.element(s)` (chốt, CHƯA code); `$component` dành sẵn = host component gần nhất (chưa cấp); `text/show/class/style` giữ tên riêng (bind:text bác); mảng trong attribute không seed — JSON island / `for` / scope literal.
- 21/09/2026 — C1 (chuỗi phép trừ §9, bước 1): kernel bỏ read-alias `data-kitwork-*` cho directive biểu thức + boundary (text/show/if/for/click/class/scope/component/model/api/item/key/debounce) và bộ giải mã IR client — 0 site nào viết dạng đó (grep toàn apps); giữ jitjs (`action/target/trigger/drag`, 59+40 chỗ đang sống) và neo gốc `data-kitwork-app/hydrate`. `render.go` `presenceRe` không nhận dạng dài nữa; `jit/js` chỉ chọn module theo `data-kit-component`. Số đo kernel.js: thô 71 816 → 70 905 byte; bỏ-comment gzip 12 098 → 11 983; MINIFIED gzip (bản production) 9 365. Sửa mục 12: gate giờ đo bản minified thật, không phải bản bỏ comment. `morph.js`/`jit/js/lib/more.js` còn đọc `data-kitwork-key` (không ai phát) — bước trừ sau.
- 21/09/2026 — ĐỢT A XONG: 12/12 mục có trạng thái; mục 11 (corpus `walk ≡ eval`) không thêm ca vì không mục nào đổi ngữ pháp BIỂU THỨC (scope không ngoặc được bọc `{}` trước khi parse; mọi thứ khác là thuộc tính/DOM). Còn treo cho đợt B nằm ở bảng "Còn treo" + "Ngoài spec" phía trên.
- 21/09/2026 — mục 8 xong theo đề xuất (cả hai runtime) và mục 12 (gate 12 KiB — lúc đó đo bản bỏ-comment, dư 190 byte; C1 đã đổi sang đo bản minified thật). Phát hiện: (1) walker kernel không ném cho tên thiếu/gọi non-function → boundary kernel chỉ thấy lỗi từ code component thật; (2) runtime component: boot render `resetDirty()` nuốt cả invalidation do boundary ghi trong lượt boot → thêm `core.booting`/`boundaryWrote`; (3) kernel trước đây một binding ném là gãy cả lượt render — `guarded()` sửa luôn.
- 21/09/2026 — mục 7 xong phần client (cả hai runtime). Phát hiện khi làm: 5 từ overlay (`count first last even odd`) đè lên field cùng tên của component LỒNG trong hàng (conformance `row-state.count` vỡ) → chốt luật: overlay lexical theo hàng, không vào host lồng; kernel tự nhiên có cùng kết quả vì chuỗi scope nearest-first. Server materialize list để B (cần quyết định nguồn dữ liệu server cho `data-kit-for`).
- 21/09/2026 — mục 6 xong. Kernel từ 1 event (`click` + 2 directive riêng + 2 attribute kèm) lên đúng họ 11 event với modifier trong TÊN thuộc tính (cùng cách đi của `data-kit-bind:<name>`: quét `getAttributeNames`, regex ở `render.go`). `:self` của runtime component không có trong spec — giữ, ghi nhận để bàn ở B.
- 21/09/2026 — mục 5 xong (trừ `$error`, thuộc mục 8). Kernel: `$this`/`$host` chính, `$el`/`$root` alias, `$event` native thật. Runtime component: `$this`/`$el`/`$host` là local của action; `$event` vẫn là ảnh chụp vì evaluator đóng — thêm 3 trường element thật; ghi vào "còn treo" để Quốc xác nhận.
- 21/09/2026 — mục 4 xong theo đề xuất (phạm vi = boundary gần nhất). Phát hiện: evaluator của runtime component là ngữ pháp ĐÓNG (chỉ own-property và bảng method cho string/number/array) nên element thô không đọc/gọi được gì — thêm bảng `ELEMENT_READS`/`ELEMENT_CALLS`; kernel thì đã mở `$el` thô từ trước. Hai bên khác độ mở, cùng ngữ pháp.
- 21/09/2026 — mục 3 xong (`data-kit-alias` là cách viết duy nhất ở cả hai runtime; alias trỏ INSTANCE, `data-kit-ref` (mục 4) sẽ trỏ element).
- 21/09/2026 — mục 2 xong (dấu `,`, ngoặc tuỳ chọn, cả hai runtime + scanner; `ideaship.md`/`ideaship-master.md`/`ideashipping.md` vẫn ghi ví dụ `;` — là hồ sơ, FINAL thắng; kernel giữ thêm dạng init `a = 1; b = 2` ngoài spec — bàn ở đợt B).
- 21/09/2026 — lập sổ; mục 1 xong (một dạng `data-kit-bind:<name>`; `data-kit-attr` bị bỏ theo quyết định của Quốc; danh sách `a: x; b: y` và dạng object của kernel gỡ hẳn — mọi site trong repo đã chuyển bằng script).
