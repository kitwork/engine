// ==========================================
// API: SHORTHAND DEFINTION
// ==========================================
// Chỉ cần 2 dòng để tạo API

// Trả về trực tiếp kết quả DB mà không cần bọc json()
work("shorthand")
    .router("GET", "/api/shorthand");

return { name: "John", age: 30 };