// ==========================================
// API: RAW PERFORMANCE REFERENCE
// ==========================================
// Minimal logic to benchmark pure engine overhead.

work("RawPerf").router("GET", "/api/raw");

return "oke";
